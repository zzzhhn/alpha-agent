"""Evolution package: methodology / factor-discovery proposer components.

FROZEN (2026-06-19, roadmap step 4 / llm-council). Do NOT expand the discovery
surface (new LLM-factor proposers, evolution strategies, sandbox capabilities)
until the product ledger (step 1) + L2 forward paper-trading (step 6) prove the
CURRENT product has forward value. The engine does not need more ways to mine
exotic weak signals right now; it needs causal memory and honest validation.
Bug fixes + maintenance of existing components are fine; net-new discovery
machinery is out of scope until the freeze lifts. See docs/ROADMAP.md.
"""
